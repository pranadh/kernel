// ../config.js accepts options via environment variables
const options = {}

if (process***REMOVED***.DOTENV_CONFIG_ENCODING != null) {
  options.encoding = process***REMOVED***.DOTENV_CONFIG_ENCODING
}

if (process***REMOVED***.DOTENV_CONFIG_PATH != null) {
  options.path = process***REMOVED***.DOTENV_CONFIG_PATH
}

if (process***REMOVED***.DOTENV_CONFIG_DEBUG != null) {
  options.debug = process***REMOVED***.DOTENV_CONFIG_DEBUG
}

if (process***REMOVED***.DOTENV_CONFIG_OVERRIDE != null) {
  options.override = process***REMOVED***.DOTENV_CONFIG_OVERRIDE
}

if (process***REMOVED***.DOTENV_CONFIG_DOTENV_KEY != null) {
  options.DOTENV_KEY = process***REMOVED***.DOTENV_CONFIG_DOTENV_KEY
}

module.exports = options
