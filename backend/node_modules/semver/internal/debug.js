const debug = (
  typeof process === 'object' &&
  process***REMOVED*** &&
  process***REMOVED***.NODE_DEBUG &&
  /\bsemver\b/i.test(process***REMOVED***.NODE_DEBUG)
) ? (...args) => console.error('SEMVER', ...args)
  : () => {}

module.exports = debug
